# BRAIN/27 Career Radar 开发周报

**周期：2026-08-03 — 2026-08-09**

**生成时间：2026-08-09（Asia/Tokyo）**

**版本基线：v0.13.0 / `aafee7a`**

## 一句话结论

本周没有新增代码提交，项目处于 v0.13.0 学术会议雷达发布后的运行观察期；本地质量门保持全绿，线上页面与会议 API 正常，但职业/论文自动巡检已出现 Worker 单次调用子请求数超限，需优先治理 Cron 批次与并发。

## 本周开发与交付

- Git 工作树干净，`main` 与 `origin/main` 一致；2026-08-03 至 2026-08-09 无新增提交。
- 当前生产基线为 v0.13.0，最近一次提交为 2026-08-02 的 academic conference radar merge；该版本包含会议数据模型、D1 迁移 `0023_academic_conference_radar.sql`、会议监控、`/conferences`、`/api/conferences`、日历整合及回归测试。
- 本地复验 `npm run verify` 通过：TypeScript、ESLint、生产构建、34/34 自动化测试全部成功。
- 线上复核通过：Worker 首页、`/conferences`、`/api/conferences`、`/api/system-status` 均返回 HTTP 200；会议 API 返回 13 个会议、18 个已确认日期。

## 线上运行观察

- 会议雷达最近一次运行（2026-08-09 02:30 UTC）：检查 12 个会议，新增 3 个官网版本证据，1 个失败，整体状态为 `partial`；系统继续保留可信种子数据，不自动改写高风险日期。
- 职业/论文 Cron 最近一次运行（2026-08-09 01:01 UTC）：检查 92 个来源，4 个内容变化，73 个失败，状态为 `partial`。
- 失败日志的主要错误为 `Too many subrequests by single Worker invocation`，说明当前单次 Worker 调用承载的来源检查量已触及平台子请求限制；系统状态接口显示当前 110 个启用来源、76 个失败来源、21 个自动观察项和 104 个待处理变更集。
- 该问题不会被表述为“自动更新正常”：当前自动监控链路已配置并运行，但覆盖率和成功率受限，需要修复批次/调度后再重新评估。

## 风险与待办

1. **P0：拆分来源巡检负载。** 按来源优先级或稳定批次拆分 Worker 调用，控制单次子请求数；同时保留每批运行记录，避免一次失败掩盖其他批次结果。
2. **P1：清理和分级失败来源。** 对持续失败、需要登录或不适合自动抓取的来源转人工核对，降低无效重试和审查队列噪声。
3. **P1：补做修复后的生产验证。** 需要看到 `trigger=cron` 的成功/部分成功运行记录，并核对失败数、变化数、候选数和审查队列趋势；不能以本地测试或受控 `trigger=test` 代替。
4. **P2：继续观察会议监控。** 对 1 个失败会议来源进行官方页面复核，确认 3 个版本证据是否需要人工确认；高风险日期仍不得自动发布。

## 下周计划

- 完成来源巡检批次与并发治理，先以小批量生产验证 Worker 子请求边界。
- 对失败来源按 HTTP/超时/平台限制分类，形成可执行的人工核对清单。
- 复核自动观察项与待处理变更集，确认稳定内容可自动收敛、风险变更仍保留人工审核。
- 在生产 Cron 连续运行后更新报告数据，决定是否进入下一版本发布。

## 证据边界

- 本周“无新增开发提交”来自仓库 `git log --since='2026-08-03 00:00' --until='2026-08-10 00:00'` 与工作树状态。
- 质量门来自本次执行的 `npm run verify`；线上状态来自 2026-08-09 对 workers.dev 公开路由和 API 的 HTTP 复核。
- 线上统计为生成周报时的快照，可能随 Cron 后续运行变化；生产运行与本地测试严格区分。
